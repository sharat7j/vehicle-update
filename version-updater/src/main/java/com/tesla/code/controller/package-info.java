/**
 * The controller package is responsible for providing all the views for the service. The APIs exposed in the controllers
 * are responsible to capture user requests and perform basic validation checks before being forward to corresponding
 * service classes to apply the business logic.
 *
 * The controller here also contains API documentation embedded in code.
 */
package com.tesla.code.controller;