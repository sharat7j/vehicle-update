/**
 * The beans package contains all the model definitions for objects that are used for both capturing user requests
 * for the APIs via Spring's default objectMapper class and also for storing entities for the in memory database.
 *
 * The schema definitions are all included as a part of the entity annotations between the different models.
 */
package com.tesla.code.beans;